{
  "name": "prisma-app",
  "version": "1.0.0",
  "scripts": {
    "dev": "tsx server.ts",
    "build": "npx prisma generate && npx prisma db push && vite build && esbuild server.ts --bundle --platform=node --format=cjs --packages=external --sourcemap --outfile=dist/server.cjs",
    "start": "node dist/server.cjs"
  },
  "dependencies": {},
  "devDependencies": {
    "prisma": "^5.0.0",
    "tsx": "^4.0.0"
  }
}
